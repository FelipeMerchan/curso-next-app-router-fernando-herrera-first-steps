/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Cuando estamos creando imágenes en Docker es recomendado
  usar el output standalone: */
  output: "standalone",
};

export default nextConfig;
